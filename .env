# ============================================
# Foodie Hub - Backend Environment Variables
# ============================================
# Update these to match your local MySQL credentials.
PORT=5000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=restaurant_db

# CORS origin(s) allowed to call the API (comma separated)
CORS_ORIGIN=http://localhost:5500,http://localhost:3000,http://127.0.0.1:5500
